# ----------------------------------------------------------------------------
# writeAgent runtime configuration (copy to `.env` and fill in values)
# ----------------------------------------------------------------------------
# All Skills and the orchestrator read LLM credentials from these env vars.
# OpenAI-compatible protocol is used so you can swap providers freely.
#
# Examples:
#   * 通义千问 DashScope (默认):  https://dashscope.aliyuncs.com/compatible-mode/v1   model=qwen-plus
#   * 智谱 GLM-4:                  https://open.bigmodel.cn/api/paas/v4               model=glm-4-flash
#   * DeepSeek:                    https://api.deepseek.com/v1                        model=deepseek-chat
#   * OpenAI:                      https://api.openai.com/v1                          model=gpt-4o-mini
# ----------------------------------------------------------------------------

WRITEAGENT_LLM_API_KEY=sk-replace-me
WRITEAGENT_LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
WRITEAGENT_LLM_MODEL=qwen-plus

# Optional: workspace root used for state.json / inputs / outputs.
# Defaults to `<repo>/.writeagent` when unset.
# WRITEAGENT_WORKSPACE=

# Optional: mock mode for offline tests / CI (no real LLM calls).
# Set to `1` to make `_shared/llm.py` return deterministic stub responses.
# WRITEAGENT_MOCK_LLM=0
